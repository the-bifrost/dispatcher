services:
  dispatcher:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - /dev:/dev
      - ./dispatcher:/app/dispatcher
      - ./config:/app/config
      - ./logs:/app/logs
    restart: unless-stopped
