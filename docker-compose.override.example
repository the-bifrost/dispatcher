services:
  dispatcher:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./dispatcher:/app/dispatcher
      - ./config:/app/config:ro
      - bifrost-data:/app/data
    restart: unless-stopped

volumes:
  bifrost-data:
